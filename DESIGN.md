# 役所の土地ウォッチ（仮）設計書 — 競売・公売・国有財産・公有財産

作成日: 2026-09-13（v2: 「棚の定義」を追加し、国有財産・公有財産を対象に加えた）
対象: Claude Code に渡す設計書。shutten（大店立地法ウォッチ）と同じ考え方・同じ部品で作る。
サイト名は仮。フォルダ名 `keibai/` も仮で、名前が決まったら変える。

## 0. 一言で

買う人から見れば「役所が土地を売っている」は同じ棚なのに、制度も窓口も違うので一覧がない。
その棚を**4段**（競売・公売・国有財産・公有財産）として定義し、大阪府・兵庫県の分を毎日ためて、
段ごとに扱いを変えて見せる。

- 競売・公売 …… 一覧に出る数字だけをためて「市区町村×月」の**統計**にする。3点セット（個票）は取らない・載せない
- 国有財産・公有財産 …… 売り手が役所自身で個人情報がないので、**個票**（住所・面積・価格・期間・結果）を載せてよい

---

## 1. 決定事項（変えない）

1. 棚は4段。行データは最初から `system`（keibai / kobai / kokuyu / koyu）を持つ。
2. **3点セットPDF**（物件明細書・現況調査報告書・評価書）は取得しない。リンクも辿らない。
3. 競売・公売の**個票は公開しない**。公開するのは集計値と「いま入札中の件数」だけ。
4. 競売・公売の**住所は市区町村まで**（政令市は区まで）。地番・住居表示・建物名は行データにも保存しない。
   突合の鍵は「裁判所ID＋事件番号＋物件番号」（公売は実施機関＋公告番号＋物件番号）。
5. 国有財産・公有財産は個票を公開してよいが、**落札者の個人名は保存も公開もしない**（法人名だけ可）。
6. 生のHTML（控え）と行データは**非公開リポジトリ**に置く。
   **このリポジトリは金庫で、ずっと private。** 公開用は履歴ゼロで別に1本作り、
   **生成したHTMLと `data/public/` の中だけ**を置く（2026-09-17 の判断。下の「公開側」）。
7. 相手のサーバーに迷惑をかけない。robots.txt を毎回確認、1リクエストごとに5秒あける（正本 3.4）、1日1回、
   User-Agent にリポジトリURL。取れなかったら**回り込まない**（名乗りを偽らない、別経路を試さない、リトライは翌日）。
8. Python 3 の**標準ライブラリだけ**。JavaScript・データベース・サーバーは使わない。
9. **秘匿ルール**（競売・公売）: 価格・倍率は、元になる件数が3件未満の区画では出さない。
10. **KSI官公庁オークションは取らない**（私企業の規約で掲載内容の権利が読み切れないため）。
    自治体がKSIで入札する回も、案内と結果は自治体のページに残るので、そちらで追う。
11. PDFは2MB以下だけ取る。写真は取らない。貸付・定期借地は対象外（売却だけ）。
12. 手元（ローカル）から実際のサイトに取りに行かない。ローカルは保存済みHTML（fixtures）で開発し、
    取得は GitHub Actions で行う。

---

## 2. なぜこの形か（判断に迷ったらここに戻る）

- BITの「サイトのご利用条件」は、掲載された**本来の趣旨以外の目的での使用**と、**改ざん・無断転載**を禁止している。
  個票の転載は正面からぶつかる。数値の統計利用が「本来の趣旨以外」に当たるかは弁護士確認事項として残す（10章）。
- BITの3点セットは個人名がマスク処理されているが、地番・室内写真・生活の様子は残る。
- BIT本体が売却結果を**過去3年分**検索できる。だから「ためた強み」が効くのは3年後。
  それまでの強みは、BITもFKRも出していない指標（取下げ率・特別売却への移行率・入札本数の分布・市区町村×月）と見せ方。
- 国税庁の公売情報サイトは**公共データ利用規約（第1.0版、CC BY 4.0互換）**で、出典を書けば加工・商用可。
  自治体は個別確認（大阪市はCC BY 4.0を明記している。ページごとに再確認）。
- 国有財産・公有財産は売り手が役所自身。差押えでも競売でもないので、誰かの不幸を並べる話にならない。
  住所・面積・価格が出せるので「○○小学校跡地 売却」「西宮市 市有地」の検索を受けられる。競売ではできなかった検索流入の柱がここで立つ。
- 国有・公有の落札結果（価格と面積）を残すと、誰も持っていない円/㎡の履歴になる。
- 個票サイトは多い（981.jp、競売公売.com、オープンポータル、アットホーム官公庁物件情報、財務省の売却情報サイト等）。
  「今出ている物件の一覧」はすでに埋まっている。空いているのは、結果を残す・4段を同じ物差しで並べる・大阪兵庫の市町まで全部拾う・円/㎡を出す、の4つ。
- 統計は「商品」ではなく「入口」。ページは数字の羅列ではなく「問いに数字で答える」形にする（8章）。

---

## 3. 棚の定義（4段）

### 3.1 4段の比較

| 段 (`system`) | 何か | 売り手 | 根拠になる制度 | 基準の値段（名前） | 落札の決まり方 | 消え方 | 個人情報 | 規約 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 競売 `keibai` | 借金の担保などを裁判所が強制的に売る | 裁判所（大阪地裁3庁・神戸地裁4庁） | 民事執行法 | 売却基準価額（買受可能価額はその8割） | 期間入札→開札。売れ残りは特別売却 | 入札終了で一覧と3点セットが消える。売却結果は過去データに3年 | 重い | BIT独自条件（目的外利用・無断転載の禁止） |
| 公売 `kobai` | 税金の滞納で差し押さえた財産を売る | 税務署（国税局）・自治体の税務課 | 国税徴収法・地方税法 | 見積価額 | 期日入札・期間入札・インターネット公売 | 公売終了で消える。入札結果はすぐ消える | 中（滞納者名は出ないが所在地は出る） | 国税庁は公共データ利用規約1.0。自治体は個別 |
| 国有財産 `kokuyu` | 国が使わなくなった宿舎跡・庁舎跡地、物納財産、国庫帰属の土地などを売る | 財務局・財務事務所（近畿財務局） | 国有財産法 | 最低売却価格（一般競争入札。公表される）／売却価格（先着順） | 期間入札・期日入札→開札。売れ残りは先着順 | 入札終了で予定物件から消える。開札結果と契約状況は別ページ | なし | 財務省（要確認。政府標準利用規約系の見込み） |
| 公有財産 `koyu` | 学校跡地・施設跡地・道路の残地などを自治体が売る | 都道府県・市町村の管財課 | 地方自治法 | 最低売却価格（自治体により予定価格） | 一般競争入札（郵送型・KSI）・先着順・プロポーザル | 回ごとに差し替え。結果は別ページに残ることが多い | なし | 自治体ごと |

### 3.2 段ごとの公開方針

| 段 | 公開するもの | 公開しないもの |
| --- | --- | --- |
| 競売 | 市区町村×月の統計、いま入札中の件数、次の開札日 | 個票、住所、事件番号、3点セット |
| 公売 | いま出ている件数と入札期日、落札÷見積価額の集計 | 個票、住所 |
| 国有財産 | 個票（住所・地番・面積・用途地域・最低売却価格・入札期間・開札日・結果・円/㎡・元ページへのリンク） | 落札者の個人名、写真、物件調書PDFの再配布 |
| 公有財産 | 同上 | 同上 |

### 3.3 共通の項目にそろえる

制度ごとに名前が違うものを、行データでは1つの項目にする。

| 行データの項目 | 競売 | 公売 | 国有財産 | 公有財産 |
| --- | --- | --- | --- | --- |
| `seller` | 裁判所名 | 国税局・税務署／自治体＋税務課 | 近畿財務局／財務事務所 | 自治体＋課 |
| `base_price` | 売却基準価額 | 見積価額 | 最低売却価格（先着順は売却価格） | 最低売却価格・予定価格 |
| `base_price_kind` | "売却基準価額" | "見積価額" | "最低売却価格" / "売却価格(先着順)" | "最低売却価格" / "予定価格" |
| `sale_price` | 売却価額 | 落札価額 | 落札価格（契約金額） | 落札価格 |
| `method` | 期間入札 / 特別売却 | 期日入札 / 期間入札 / インターネット | 期間入札 / 期日入札 / 先着順 | 一般競争入札(郵送) / 一般競争入札(KSI) / 先着順 / プロポーザル |
| `ratio` | 売却価額÷売却基準価額 | 落札÷見積価額 | 落札÷最低売却価格 | 落札÷最低売却価格 |

同じ物差し＝ `ratio`（落札÷基準価格）。4段で並べて「役所の値付けに対して市場はいくら払うか」を見せる。

### 3.4 すでにある棚と空いている場所

- アットホーム「官公庁物件情報」: 公売と国・公有財産売払を扱う。官公庁から依頼を受けた分で、公開しているのは一部
- 財務省「国有財産の売却情報サイト」: 全国の財務局の一般競争入札物件と先着順物件を検索できる（アットホームの空き家バンク基盤）
- KSI官公庁オークション: 公売と公有財産売却が別カテゴリ
- 近畿財務局「地方公共団体（管内）の公有地売却情報」と国交省「公的不動産（PRE）売却・貸付け情報」: 自治体ページへのリンク集。**収集先の種を蒔く索引に使う**
- 空いている場所: 結果（落札価格）のアーカイブ、4段の同じ物差し、大阪兵庫の市町まで全部、円/㎡

---

## 4. リポジトリ構成

2つのリポジトリに分ける。

| リポジトリ | 公開 | 役割 |
| --- | --- | --- |
| `VirgoB77/keibai-toukei-raw`（ここ） | **ずっと非公開** | 金庫。収集・保存・行データ・集計。**Actions は公開用が持つ**（移し終わるまでは、ここが唯一の集め手） |
| 公開用（**未作成**） | 公開 | 履歴ゼロで新しく作る。生成物だけを受け取り、GitHub Pages で公開。**Actions はこちらで動く** |

`VirgoB77/ic-log` に相乗りする案は採らない（2026-09-17）。名刺やテナント誘致など
別の物が入っていて、site の名乗りもぶつかるため。

金庫の中身:

```
DESIGN.md                        この文書
README.md                        使い方（shutten/README.md と同じ調子で書く）
sources.json                     見に行く先（5章）
recon.py                         偵察。shutten/recon.py を写して直す
fetch.py                         取得（一覧ページ・2MB以下のPDF）
parse.py                         HTML/PDF → 行データ
aggregate.py                     行データ → 集計JSON（秘匿ルール込み）
build.py                         集計JSON・公開用行データ → 静的HTML（インラインSVGのグラフ）
（publish.sh は作らない。公開用の Actions が金庫を checkout して自分に push する）
inbox/                           人が手で保存したHTMLを置く場所（9章）
inbox/done/                      片づけ用（自動では移さない）
data/raw/<id>/<日付>.html         取ってきたページそのまま（控え。再公開しない）
data/rows/keibai/<court_id>.json 競売の行データ（裁判所ごと）
data/rows/kobai/<id>.json        公売の行データ
data/rows/kokuyu/<id>.json       国有財産の行データ
data/rows/koyu/<id>.json         公有財産の行データ（自治体ごと）
data/agg/                        **社内用の集計。公開しない**（下記）
data/agg/monthly.json            市区町村×種別×段階×月
data/agg/kaisatsu.json           開札の回（裁判所・庁×月・庁×年・府県×月…）
data/agg/shuroku.json            どこまで見たか（収録範囲）
data/public/                     **ここだけが公開してよい置き場**
data/public/index.json           姉妹サイトに渡す一覧（counts_by_city のみ）
data/public/kokuyu.json          公開用の個票（国有財産）
data/public/koyu.json            公開用の個票（公有財産）
data/recon-report.md             最後の偵察結果
tests/                           unittest。fixtures/ に保存済みHTML（競売の分は住所を含むので非公開リポにだけ置く）
.github/workflows/keibai.yml     毎日 07:30 JST（cron "30 22 * * *"）。shutten の 07:00 とずらす
```

**`data/agg/` と `data/public/` の違いは、粗さが混ざっているかどうか。**

`data/agg/` には、同じ数が2つ以上の粗さで入っている（種別あり・種別なし、
庁×月・庁×年・府県×月…）。どの粗さで出すかを選ぶのはまだ先なので、
選べるように並べてある。1つ1つの升は 3.2 を守っている（親の升は落とし、
兄弟はそろえ、1〜2件は伏せてある）が、**並べてあるものを2つ出すと、
引き算で伏せた升が戻る**（正本 3.2）。守れていないのはファイル全体の
ほうなので、升ごとの手当てでは直らない。置き場所で分ける。

だから `data/agg/*.json` には `"公開しない"` の欄を必ず入れる
（`common/shukei.py` の `NOT_PUBLIC`）。`tests/test_public.py` が見張る。
公開するときは、そこから**1つの粗さだけ**を選んで `data/public/` に写す。

## 公開側

**名前は決まった**（2026-09-18、運営者の決定）。
**リポジトリは2つとも在る**（2026-09-19）。

| | 決めたもの |
| --- | --- |
| 独自ドメイン | `keibai-toukei.com` |
| `site_id` | `keibai-toukei` |
| `id` の接頭辞 | `keibai`（正本 2節の表で衝突しないことを確かめた） |
| 公開用リポジトリ | `keibai-toukei`（public） |
| 金庫（ここ） | `keibai-toukei-raw`（private） |

**一度決めたら変えない**（正本 9節「鍵にはあとから変わらないものだけを入れる」）。

### 当てたもの・まだのもの（2026-09-19）

**⓪（アカウント Settings → Pages → Verified domains）は済んだ。**
WHOIS は代理公開。

| | 当てたか | |
| --- | --- | --- |
| `site_id` = `keibai-toukei` | ✅ 09-18 | `records` が0件のうちに |
| `id` の接頭辞 = `keibai` | ✅ 09-18 | 同上 |
| UA = `kujiraya archive bot (keibai-toukei; …)` | ✅ 09-19 | ⓪が済んだので据え置きを解いた |
| 金庫を `keibai-toukei-raw` に rename | ✅ 09-19 | ①（人） |
| 公開用 `keibai-toukei` を履歴ゼロで作る | ✅ 09-19 | ②（人） |
| 公開用の木を入れる（手順3） | ✅ 09-19 | 42ファイル・1コミット |
| 名前による自己停止（`*-raw` なら止まる） | ✅ 09-19 | 下の「見張り」 |
| deploy key（④） | **まだ** | 運営者が作る。入ったら公開用を1回通す |
| Pages に `keibai-toukei.com`（⑤） | **まだ** | ④のあと |
| `site_url` | **まだ** | ⑤のあと。下記 |
| 金庫から `.github/workflows/` と印を消す | **まだ** | **公開用が1回通ってから**（下の「見張り」） |

**`site_url` だけ、⓪のあとでもまだ入れない。**

⓪は「**ほかの人にドメインを取られない**」ことを保証するだけで、
「**そこに何かがある**」ことは保証しない。2026-09-19 時点で
`keibai-toukei.com` は DNS が引けない（`curl` で確認）。

正本 3.4「**404 を入れない。** まだ公開していないページのURLは入れない。
確認できない名乗りは、名乗らないより不審に見える」。

**Pages が配信を始めた日に入れる。** UA を先に当てられたのは、
そちらが URL ではなく**名前**だから。同じ「⓪のあと」でも、
名前と URL では確かめられる時期が違う。

### 「公売」を名前に入れなかった理由と、その手当て

ドメインに「公売」を入れるかを検討したうえで、**入れない**と決めた。

- Google の exact match domain は「one of many factors」で、ほぼ効かない
  （公式ドキュメントに「don't give too much credit」と明記）
- **公売で検索する人は、たいてい本家（官公庁オークション）に行く。**
  こちらは個票を出さない統計サイトなので、客層が違う
- 狙うのは「競売 落札率 大阪」「公売 落札価格 相場」のような語で、
  **これはページのタイトルと見出しで捕まえる**

**だから、名前で落ちたぶんを中身で補う。必ずやること。**

- `/koubai/` の専用ページを作り、`title` `h1` `meta description` に「公売」を入れる。
  `sitemap.xml` にも入れる
- **`about` と `README` の最初に「公売も扱う」と書く。**
  読む人が「公売は無いのか」と誤解するほうが、検索順位より痛い

**売り文句にしない**（正本 3.3）。「狙い目」「安く買える」「穴場」は書かない。
書くのは「何を数えているか」だけ。

```
（公開用）/index.html            棚の全体。Phase 3 はこの1枚だけ
（公開用）/koubai/index.html     公売の専用ページ（title・h1・description に「公売」）
（公開用）/data/index.json       ← data/public/ から写したものだけ
（公開用）/data/kokuyu.json      国有財産（Phase 4）
（公開用）/data/koyu.json        公有財産（Phase 4）
```

### このリポジトリは金庫。公開用は別に、履歴ゼロで作る

**金庫を public にしない。** ここには入札中の物件の所在地が生のまま入る。
git の履歴は消せないので、public にした日に**過去のぶんまで全部読める**。
ファイルを消すコミットを積んでも、履歴から取り出せる。

履歴を書き換えて public にする手は**採らない**。姉妹サイト（大型店日報）が調べて、
こうだった（正本 9節）。

- `refs/pull/*` は force-push で消えない
- fork とキャッシュに残る
- **PR の題名と本文は履歴の外に残る**

| | 置くもの | |
| --- | --- | --- |
| 収集用（ここ） | 生データ・`inbox/`・人が手で入れた原本・走らせた記録 | **ずっと private** |
| 公開用（未作成） | 集計結果・`index.json`・HTML・Pages | public。**履歴ゼロで作る** |

**このサイトは条件がいい。まだ一度も public にしていない。**
大型店日報は public にしてから気づいたので、rename して作り直す手順が要った
（正本 9節の ⓪〜⑨）。こちらはそれが要らず、公開用を新しく1本作るだけで済む。

### 仕組み（正本 9節。大型店日報の `shutten-recon.yml` が実物）

- 走るのは**公開用の Actions**。public なので Actions は無料で、`GITHUB_TOKEN` で
  自分に push できる。**収集用は置き場**。収集用の Actions は止め、workflow を持たせない
- 公開用の Actions が **deploy key** で収集用を `_raw/` に checkout する。
  公開鍵は収集用の Settings → Deploy keys（書き込み可）、
  秘密鍵は公開用の Secret **`RAW_DEPLOY_KEY`**。パスフレーズは空（Actions は対話できない）。
  **PAT は期限で黙って止まるので使わない。** deploy key は1本のリポジトリにしか効かない
- Secret は**生データを持たない公開用にだけ**入れる
- 公開用の Actions は走る前に、収集用が公開側から見えない（private）ことを確かめ、
  見えたら生データを送らずに止まる
- `data/raw` などを `ln -s` で**今までのパスにつなぐ**。Python は置き場の違いを知らない。
  **`inbox/` と `tests/fixtures/` も金庫に残す。** 人が手で保存したページと、
  そこから作ったテスト用のHTMLには、物件の所在地が生のまま入っている
- 公開用への commit は**許可リスト**。公開してよいと決めたパスだけを列挙して `git add` する。
  `git add data` のような丸ごとはしない（足し忘れは「載らない」で済む。逆は済まない）。
  **除外リストにしない。** 「作る」ときは同じに見えるが、「入れ替える」ときに
  相手が足したものまで消す（姉妹サイトが実物で踏んだ）
- **許可リストが掛かるのはパスだけ。本文には正本 3.2 が掛かる。**
  コミットのメッセージと **PR の題名・本文**に、升の実数・引き算の式・場所の名前を
  書かない（PR の題名と本文は履歴の外に残るので、force-push でも消えない）。
  形で書く。見本は `kaisatsu.py` の冒頭のコメント（`合計.◯◯ = null` の書き方）
- **`CNAME` を許可リストに入れない。** Pages に独自ドメインを付けると
  GitHub が公開側に commit する。こちらが木に入れて `git add -A` すると
  削除の差分になり、ドメインが外れる。**仕掛けではなく「入れないこと」が守り**
- **`robots.txt` と `sitemap.xml` は許可リストに入れる。** URL から作るので、
  URL を差し替えたときに公開側へ届かないと古いまま残る
- **URL に連動するものは `common/site.json` の1か所から作る**
  （UA・`robots.txt` の `Sitemap:` 行・`sitemap.xml`・個票のURL）
- しまう順番は **収集用 → 公開用**。取り直せないものを先にしまう
- ページを作ったあとに**もう一度テストを通してから**公開用に push する
- 走らせた記録（`data/*.md`）は生の値を含みうる。守り方は2つあり、正本 9節が両方載せている。
  **いまは「先頭に1行」**（`common/report.py` の `NOT_PUBLIC_LINE`、
  `tests/test_public.py` が見張る）。1本のリポジトリの中で分けているので、
  ファイル単体を開いた人にも分かるこちらが向く。
  公開用を作って収集用と別々に動き出したら、**収集用の `data/reports/` に写す**ほうへ移り、
  先頭の1行は二重の歯止めとして残す（移す形だと、移し忘れた1本が普通の顔で残る）。
  この節は競売統計が正本に足したもの（正本 9節・2026-09-17）

### 向きが変わった（2026-09-17）

前は「private の keibai-data で Actions が走り、公開側へ push する」と書いてあった。
**正本 9節は逆で、走るのは公開用の Actions。** 公開用は public なので Actions が無料で、
`GITHUB_TOKEN` で自分に push できる。収集用は置き場に徹する。

変わるのは3つ。

| | 前 | いま |
| --- | --- | --- |
| Actions が走る場所 | keibai-data（private） | 公開用（public） |
| 鍵 | ic-log 宛ての fine-grained PAT（`IC_LOG_TOKEN`） | **deploy key**（`RAW_DEPLOY_KEY`） |
| 向き | 金庫 →（push）→ 公開 | 公開 →（checkout）→ 金庫を隣に出す |

PAT をやめるのは、**期限が来ると黙って止まる**から。deploy key は1本のリポジトリにしか
効かないので、漏れたときの範囲も狭い。

### 移植元はまだ走っていない

大型店日報の同じ仕組みは、**まだ main に入っていない**（2026-09-17 時点で
未マージの枝 `chk/claude/vault-prep` にある）。一度も本番で走っていないので、
そのまま写す前に、向こうが1回走るのを待つか、こちらを先行させるかを決める。

### いまはまだ1本

鍵が無いあいだ1本の形（`git add data` の丸ごと）で動いてよいのは、
**生データを既に追跡している移行前のリポジトリだけ**（正本 9節）。ここはそれに当たる。
だから毎朝の workflow はこのままでよい。公開用を作った日から上の形に移る。

**移る前にやること**（正本 9節の本文から、このサイトに要るものだけ。順番も正本どおり）。
⓪〜⑨ の作り直しは要らない。一度も public にしていないため。
**1〜4 は済んだ**（2026-09-19）。いま止まっているのは 6（鍵）。

1. **公開用の名前を決める。** 独自ドメインのラベルと同じにする。
   `site_id` もこれにする（**リポジトリ名を使わない**。リポジトリ名は
   この手順の2で変わる。姉妹サイトは同じ理由でドメインの語幹に変えた）
2. アカウントの Settings → Pages → **Verified domains** で独自ドメインを検証済みにする。
   作り直さない場合も先にやる（検証していないと、他人が自分の Pages に付けられる）
3. 公開用を**履歴ゼロ**で作り、公開用の木を1コミットで入れる。
   **入れないもの**: `data/raw` `inbox` `data/rows` `data/schedule` `data/terms`
   `data/agg` `data/cross` `data/*.md` `tests/fixtures` `CNAME`
4. このリポジトリを **`<公開用の名前>-raw`** に rename
   （workflow の checkout・置き場の確認・名前による自己停止が、全部この名前を見る）
5. **すぐ private を確かめ**、このリポジトリの main から `.github/workflows/` を消す
   （収集用に workflow を持たせない）。**`.github/ikou-mae` も同じコミットで消す。**
   印が残っていると、戻された workflow がそのまま通ってしまう。
   消すのは**公開用の Actions が1回通ってから**（それまでここが唯一の集め手）
6. 収集用に deploy key（書き込み可）、公開用の Secret `RAW_DEPLOY_KEY` に秘密鍵。
   **Secret を入れるのは、収集用が private であることを確かめた後**
7. 公開用の Pages に独自ドメインを付ける（GitHub が `CNAME` を commit する）
8. `https://<owner>.github.io/<公開用の名前>-raw/data/raw/<id>/<日付>.html` が
   **404 であることを見る**

**公開用の木を作る前に**、評価の語（正本 3.3）を1つずつ言い換える。
姉妹サイトの `check_copy.py` を写すと、いまの木は21か所で落ちる。
どれも地域の評価ではなく設計の言葉なので、言い換えれば済む。**例外リストを作らない。**

**古い clone は全部 remote を付け替えるか消す。** 同じ名前を公開用が使うので、
古い clone から push すると、旧履歴ごと public に戻る。

### 前身の置き場（2026-09-17 に確かめた。**きれいだった**）

正本 9節「前身の置き場を見る」。作り直しの手順は「いま書いているリポジトリ」が
対象で、**その前に使っていた置き場**は対象外になる。
姉妹サイト（大型店日報）が、前身の public なリポジトリに
**伏せる前の出力 3,525枚**が履歴から読める状態で残っていたのを見つけた。

**このサイトは、どこからも分かれてきていない。** 確かめたのは5つ。

| 見たこと | 結果 |
| --- | --- |
| 親の無いコミット（ルート）の数 | **1つだけ**。よそから履歴を持ち込んでいない |
| 最初のコミットの中身 | 12ファイル。`DESIGN.md` `README.md` `recon.py` `sources.json` `tests/` と `.gitkeep` だけ。**データは1件も無い** |
| `ic-log`（`publish.sh` の宛先だった既存のリポジトリ） | 全履歴 165 コミットに `keibai/` のパスが**一度も無い**。いまは private・fork 0 |
| ほかの public なリポジトリ | `ogataten-nippo` `gaitou-settou` `maisoku` のいずれにも競売・公売のものは無い |
| `data/public/index.json` の全履歴 | **どの版も `records: 0`**。升は 0 → 40 → 92 と増えたが、伏せ字は全部入っている |

`index.json` の `site` が `"ic-log"` になっていたのは、
`make_index.py` の既定値がそう書いてあっただけだった。

    SITE = os.environ.get("SITE", "ic-log")
    BASE_URL = os.environ.get("SITE_URL", "https://virgob77.github.io/ic-log/keibai/")

**送る先として書いてあっただけで、一度も送っていない**（`publish.sh` は存在しない）。
2026-09-17 に `keibai-data` / 競売統計 に直した。

伏せる前の値が入っているのは `data/raw/` `inbox/` `data/rows/` の3つで、
**どれもこのリポジトリの中だけ**にある。ここは private のまま。

fork は 0。コピーは存在しない。

### このサイトにしか無かった穴（2026-09-18）

正本 11節「**上げる前に、ほかのサイトの実物を見る**」。
効きそうだから送ると、**無い穴の見張りをほかのサイトに足させる**ことになる。
足した見張りは鳴らず、鳴らない見張りはそのうち読み飛ばされる。

**ただし、確かめることは報告をためらう理由にならない。**
見るのは「ほかにもあるか」であって、「報告してよいか」ではない。
1サイトにしか無ければ、その1サイトで直す。
**確かめたことも含めて報告すれば、次に同じ形が出たとき2サイト目として上がる**
（正本 5節の線3）。だからここに残す。

| 見つけたもの | 確かめた相手 | 結果 |
| --- | --- | --- |
| 設定（`common/site.json`）が読めない日に、黙って既定値へ落ちて**古い名前・連絡先無し**で名乗る | 街頭窃盗統計 `scripts/config.py` | **そちらは定数の直書きで、読み込みが無い。落ちようがない** |

`common/site.py` はこのサイトにしか無い。だから正本には上げず、ここで直した
（既定値から名前を消し、読めなければ止まる）。

**次に別のサイトが「設定が読めない日に既定値へ落ちる」を報告してきたら、
その時点で2サイト目。** そのとき正本に上げる。

### 見張り

毎朝の workflow の最初に、取りに行く前の確認が2つある。

1. **「金庫では走らないことの確認」**（`github.repository` の名前が `-raw` で終わるか）
2. **「このリポジトリが非公開であることの確認」**（`github.event.repository.private`）

設定が変わったら、翌朝そこで止まる。止まれば、その日の分は増えない。

1つめは**移したあとに効く保険**。「金庫に workflow が無ければ走りようがない」は
そのとおりだが、**あとで誰かが merge や copy で金庫に戻す**経路が残る
（姉妹サイトの `*-raw) exit 1` が実物。2026-09-19 に正本側から）。

移し終わるまでは、ここが唯一の集め手。名前だけで止めるとその朝の物件が
永久に取れない（BIT は消える。**母集団の保存が先**）。だから
**`.github/ikou-mae` があるあいだだけ通す**。印があるあいだは毎回 `::warning::` を出し、
残っていることが実行の記録に見える形にしてある。手順5でこの印を消した日から、
名前だけで止まる。

見張りは `tests/test_workflow.py`。**grep で確かめない。**
workflow から `run:` の本文を取り出して、置き場の名前と印のあるなしを入れ替えながら
**実際に `sh` に通す**（`"TZ" in text` は `env:` を消しても通った。あれと同じ形にしない）。

正本 9節に新しく入った決まりが4つ。**どれも「道具は正しいのに黙る」話。**

1. **検査は最初の1件で止まらない形にする。** 止まると2件目以降が隠れる
   （姉妹サイトの検査が、最初に落ちた1つしか見えていなかった）
2. **鳴らせていない見張りは、動くと分かっていない見張り。**
   検査を足したら、**わざと壊して鳴らし、直して黙る**ところまで見る。
   **そのとき `python3 -B` で走らせる**（2026-09-19 に踏んだ）。
   漢字2文字を漢字2文字に書き換えると**バイト数が変わらない**ので、
   `__pycache__` の古い版がそのまま使われることがある。
   **壊したのに鳴らない**、**直したのに鳴り続ける**、のどちらも起きる。
   壊して鳴らす手順そのものが、嘘をつく
3. **退役した置き場の名前とURLを、コードに書き置かない。**
   退役したものは一覧（`RETIRED`）に置いて消さない。現在値と比べない
4. **実測値を説明文に書かない。** 「いま成り立っている値」は書かず、
   「2026-09-17 にこう起きた」は書いてよい（DESIGN と決定ログの書き分け）

公開用を作ったら、**検査は2回走らせる**。「作る → 収集用にしまう →
**もう一度検査** → 公開用にしまう」。1回目は昨日の出力に対してなので、
それだけだと今日の伏せ忘れが翌朝まで公開される。
いまの `if: always()`（取り直せないものを先にしまう）は、その考え方の先取り。

**出した木で通しても足りない**（2026-09-19）。`make_public_tree.py` が作る木には
`.git` が無いので、**git を見る検査はそこでは skip する。** skip は通ったのと
区別がつかない。作者欄の見張り（`tests/test_meiwaku_address.py`）が、
公開用の clone で毎回落ちていたのに、出した木では skip していて見えなかった。
名簿はこの金庫の履歴のもので、履歴ゼロの公開用には1つも無いため。

**公開用の Actions は取りに行く前にテストを通す。**
この1本のせいで、1回も通らないまま止まるところだった。
だから**公開用に push する前に、公開用を clone して、そこで走らせる。**

**そして、走らせたあとにもう一度見る**（2026-09-19）。
木を作った直後の `check()` は、**そのあと増えたものを見ていない。**
実際に踏んだ。公開用を clone してテストを通したら、そのテストが
`data/parse-unknown.md` を書き、**そのまま公開用に1コミット入った。**
中身はテストが作った架空の1行で、実在の物件・住所・事件番号は
含まなかったが、置き場が違う。git の履歴からは消えない。

    git clone <公開用> /tmp/pub && cp -r <出した木>/. /tmp/pub/
    cd /tmp/pub && python3 -m unittest discover -s tests   ← ここで増える
    python3 scripts/make_public_tree.py --check /tmp/pub   ← **だから最後に見る**
    cd /tmp/pub && git add -A . && git commit && git push

**作るときと出すときは、別の瞬間。** 作るときに見ただけでは足りない。

**数が合わなかった日は、公開側に出さない**（2026-09-19）。
`make_index.py` はその日の `index.json` を書き直さないので、
**前の日のものがそのまま残る。** 中身は古いが `generated_at` も古いので
嘘は言わない。ただし「今日は止まった」はログを見ないと分からないので、
`::error::` で Actions の注記に出している。
公開用の workflow を書く日には、**止まった日は push しない**を入れること。

---

## 5. 情報源（sources.json）

形式は `shutten/sources.json` と同じ（`id`, `name`, `area`, `url`, `enabled`, `note`）。
追加項目:

| 項目 | 意味 |
| --- | --- |
| `system` | keibai / kobai / kokuyu / koyu |
| `kind` | 下の各表の kind |
| `court_id` | BITの裁判所ID（競売だけ） |
| `manual` | `true` なら Actions で取りに行かず、inbox に置かれたHTMLだけを読む |
| `pdf` / `pdf_max_mb` | shutten と同じ。BIT は必ず `pdf: false`。ほかは 2MB |

### 5.1 競売（BIT）

裁判所と管轄（BITの単位。court_id は BIT の裁判所別ページ番号と一致している様子だが、
未確認のものは BIT の「裁判所一覧（近畿）」を人が開いて埋める）:

| court_id | 裁判所 | 主な管轄 |
| --- | --- | --- |
| 33311 | 神戸地方裁判所 本庁 | 神戸市全域・明石・三木・三田・洲本・南あわじ・淡路 |
| 33331 | 神戸地裁 尼崎支部 | 尼崎・西宮・芦屋・伊丹・宝塚・川西・猪名川・丹波篠山・丹波 |
| 33332 | 神戸地裁 姫路支部 | 姫路・加古川・高砂・たつの・西脇・小野・加西・加東ほか播磨一帯 |
| 未確認 | 神戸地裁 豊岡支部 | 但馬 |
| 未確認 | 大阪地裁 本庁 | 大阪市ほか |
| 未確認 | 大阪地裁 堺支部 | 堺市ほか南河内 |
| 未確認 | 大阪地裁 岸和田支部 | 泉州 |

URLの手がかり（分かっているもの）:

- トップ: `https://www.bit.courts.go.jp/app/top/pt001/h01`
- 売却スケジュール（裁判所別。**普通のURLでGETできる**）: `https://www.bit.courts.go.jp/app/schedule/pr005/h01?courtId=33311`
- 物件検索・売却結果・過去データ・取下げ等の検索は画面遷移（フォーム）。GETで再現できるかは偵察で確かめる。
  できなければ inbox（手保存）に回す。
- ご利用条件: `https://www.bit.courts.go.jp/policy/site_policy.html`
- 使い方（列名の確認用）: `https://www.bit.courts.go.jp/help/index.html`

各裁判所につき取るもの:

| kind | 何が取れるか | 使い道 | 自動／手 |
| --- | --- | --- | --- |
| bit-schedule | 公告日・閲覧開始日・入札期間・開札期日 | 開札回（コホート）の定義、次回予定 | 自動（GET） |
| bit-list | 入札中・閲覧中の物件一覧（種別・所在地・売却基準価額・買受可能価額・入札期間） | 公告件数、消えた物件の検出 | 偵察しだい |
| bit-result | 開札結果（売却価額・入札本数など） | 乖離率・成立率・入札本数 | 偵察しだい |
| bit-withdrawn | 取下げ等の検索 | 消えた物件の裏取り | 偵察しだい |
| bit-past | 売却済み物件（過去3年） | 初回の3年分バックフィル | 手（初回のみ） |

BIT からは**3点セットを絶対に取らない**。`fetch.py` に次のフィルタを持ち、テストで確認する:

- リンク文字に「3点セット」「ダウンロード」を含むリンクは辿らない
- `bit.courts.go.jp` からの応答が `application/pdf` なら保存せず捨て、レポートに「PDFを弾いた」と出す

### 5.2 公売

| id | url | 何が載るか | 規約 |
| --- | --- | --- | --- |
| kobai-nta-home | `https://www.koubai.nta.go.jp/auctionx/public/hp001.php` | 新着・お知らせ・登録中件数 | 公共データ利用規約1.0 |
| kobai-nta-fudosan | `https://www.koubai.nta.go.jp/auctionx/public/hp0241.php` | 不動産の検索結果（見積価額・所在・公売日）。大阪・兵庫に絞る地域パラメータは偵察で確認 | 同上 |
| kobai-nta-kekka | `https://www.koubai.nta.go.jp/auctionx/public/hp001_01.php?pageid=0&doc=17` | 入札結果（公告日単位） | 同上 |
| kobai-nta-net-kekka | `https://www.koubai.nta.go.jp/auctionx/public/hp001_01.php?pageid=0&doc=27` | インターネット公売等の結果 | 同上 |
| kobai-nta-schedule | `https://www.koubai.nta.go.jp/auctionx/public/hp001_01.php?doc=16` | 今後の公売スケジュール | 同上 |
| kobai-osaka-pref | `https://www.pref.osaka.lg.jp/o050040/zei/alacarte/koubaijohou.html` | 府税の公売情報 | 要確認 |
| kobai-hyogo-pref | `https://web.pref.hyogo.lg.jp/kk22/koubainooshirase.html` | 県税の会場公売・インターネット公売のお知らせ | 要確認 |
| kobai-osaka-city | `https://www.city.osaka.lg.jp/kurashi/category/3009-8-0-0-0-0-0-0-0-0.html` | 市税の差押財産の公売 | CC BY 4.0（ページごとに再確認） |
| kobai-kobe-city | URL未確認 | 市税の公売 | `enabled: false` で登録し、note に「URL未確認」 |

- 国税庁のページは GET のURLで開ける。件数が少ないので1日数リクエスト。
- 公売公告PDFは 2MB以下だけ取る。所有者名が載っていても parse で名前を取り出さない（rows に入れない）。
- 国税庁の物件は「個人情報保護のため写真を一部修正」している。写真は取らない。

### 5.3 国有財産（近畿財務局）

| id | url | 何が載るか | kind |
| --- | --- | --- | --- |
| kokuyu-kinki-yotei | `https://lfb.mof.go.jp/kinki/kanzai/pagekinkihp029000143.html` | 一般競争入札予定物件（売却）。「次回入札で入札予定の物件」と「今後入札を予定している物件」 | kokuyu-list |
| kokuyu-kinki-kekka | `https://lfb.mof.go.jp/kinki/kanzai/np-bid-result_00001.html` | 一般競争入札（売却）の開札結果及び契約状況。受付終了の翌営業日から開札までは応札の有無も出る | kokuyu-result |
| kokuyu-kinki-senchaku | URL未確認（「すぐに購入できる物件」＝先着順の売却物件） | 先着順の物件と売却価格 | kokuyu-list |
| kokuyu-kinki-top | `https://lfb.mof.go.jp/kinki/kanzai/pageknkhp00200005.html`（旧ドメイン `kinki.mof.go.jp` からの移行先。要確認） | 国有財産の売却情報の入口 | index |

- 近畿財務局の期間入札は回数が少ない（年1〜2回の見込み）。先着順は随時。
- 物件調書PDFはリンクだけ残す（大きいものが多い）。2MB以下なら取る。
- 参照だけ（取らない）: 財務省「国有財産の売却情報サイト」`https://kokuyuzaisan.akiya-athome.jp/`（アットホーム基盤。規約が別）、
  財務省「国有財産の売却情報」`https://www.mof.go.jp/policy/national_property/summary/sellout/index.htm`（国有財産情報公開システムへの入口。要確認）

### 5.4 公有財産（自治体）

まず府県・政令市。市町は 5.5 の索引から偵察して、一覧のある所から足す。

| id | url | 何が載るか | kind |
| --- | --- | --- | --- |
| koyu-osaka-pref | `https://www.pref.osaka.lg.jp/o050050/kanzai/baikyakutou/index.html` | 府有地等の入札・先着順による売却。今後の入札予定、先着順物件、一般競争入札の結果、サウンディング。一般競争入札はKSIで実施（案内と結果は府ページに残る） | koyu-list / koyu-result |
| koyu-hyogo-pref | `https://web.pref.hyogo.lg.jp/kk30/pa10_000000015.html` | 県有地の売却。一般競争入札（郵送型）のお知らせPDF（6〜8MB。取らない、リンクだけ）、先着順物件 | koyu-list |
| koyu-hyogo-pref-kekka | `https://web.pref.hyogo.lg.jp/kk30/pa10_000000039.html` | 一般競争入札による県有地売払い結果 | koyu-result |
| koyu-hyogo-pref-cat | `https://web.pref.hyogo.lg.jp/pref/cate3_618.html` | 県有財産売却のカテゴリ（インターネット一般競争入札の案内など） | index |
| koyu-osaka-city | URL未確認（大阪市「市有不動産売り払い」。大阪府ページからリンクあり） | 市有不動産の売払 | koyu-list / koyu-result |
| koyu-kobe-city | `https://www.city.kobe.lg.jp/a59688/shise/kobai/sell/index.html` | 市有地売却・貸付関係の新着（複数の局にまたがる） | index |
| koyu-kobe-city-nyusatsu | `https://www.city.kobe.lg.jp/a59688/shise/kobai/sell/nyusatsu00.html` | 入札による売却（回ごとのページと開札月） | koyu-list |
| koyu-kobe-city-r7 | `https://www.city.kobe.lg.jp/a59688/r7ippan.html` | 令和7年度一般競争入札による神戸市有地売却（物件・期限の例） | koyu-list |
| koyu-sakai-city | URL未確認 | 堺市の市有地売却 | `enabled: false` |

- 貸付・定期借地・サウンディング（意見募集）は取らない。売却だけ。
- 自治体の「結果」は契約課の入札結果ページなど別の場所にあることが多い。偵察で探し、見つからなければ note に書く。

### 5.5 索引（種まき用。取り込まず、人が見て sources.json に足す）

- 近畿財務局「地方公共団体（管内）の公有地売却情報」: `https://lfb.mof.go.jp/kinki/np-localgovernment-link_00001.html`
- 国交省「公的不動産（PRE）売却・貸付け情報」: `https://www.mlit.go.jp/totikensangyo/totikensangyo_tk5_000166.html`
- 兵庫県の県有地ページ（市町有地の売却情報へのリンクがある）

---

## 6. 行データ（非公開）

全段で共通の項目:

```json
{
  "system": "keibai",
  "key": "33331:令和7年(ケ)第123号:1",
  "seller": "神戸地方裁判所尼崎支部",
  "kind": "マンション",
  "kind_raw": "マンション",
  "pref": "兵庫県",
  "city": "西宮市",
  "method": "期間入札",
  "base_price": 12000000,
  "base_price_kind": "売却基準価額",
  "sale_price": 18500000,
  "ratio": 1.54,
  "area_sqm": null,
  "price_per_sqm": null,
  "bid_start": "2026-09-16",
  "bid_end": "2026-09-24",
  "open_date": "2026-10-01",
  "bidders": 7,
  "status": "売却",
  "first_seen": "2026-09-13",
  "last_seen": "2026-10-01",
  "sources": ["bit-list", "bit-result"],
  "source_url": null
}
```

段ごとの追加項目:

| 段 | 追加項目 | 持たない項目 |
| --- | --- | --- |
| 競売 | `court_id`, `case_no`, `item_no`, `min_price`（買受可能価額） | 住所（市区町村より細かいもの） |
| 公売 | `agency`, `notice_no`, `item_no` | 住所、所有者名 |
| 国有財産 | `address`（住居表示・地番）, `area_sqm`, `zoning`（用途地域）, `docs`（物件調書等のURL）, `round`（令和7年度第1回など）, `winner_kind`（法人/個人/不明）, `winner_name`（法人だけ）, `source_url` | 落札者の個人名、写真 |
| 公有財産 | 同上 ＋ `former_use`（学校跡地など、書いてあれば） | 同上 |

- `kind` は 土地／戸建て／マンション／その他 に正規化。原文は `kind_raw` に残す
- `status`: 予定 → 公告／受付中 → 入札中 → 売却／落札 ／ 不売／不落 ／ 取下げ／中止 ／ 変更 ／ 特別売却／先着順 ／ 消えた（裏取り待ち）
- 価格は円の整数。不明は `null`。`ratio` は `sale_price ÷ base_price`、`price_per_sqm` は `sale_price ÷ area_sqm`（面積があるときだけ）
- 住所を市区町村までに落とす関数 `to_city(text)` を1か所に置く。競売・公売の rows に書く前に必ず通す。
  テストで「町丁目・番地・建物名・部屋番号が残らない」ことを保証する。政令市（大阪市・堺市・神戸市）は区まで
- 過去データ由来の競売の行は `sources: ["bit-past"]`、status は「売却」。
  **取下げは過去データからは分からない**（統計の但し書きに書く）
- `winner_name` は「株式会社」「有限会社」「合同会社」「一般社団法人」などを含むときだけ入れる。それ以外は `null`。テストで固定
- `case_no` と `key`（競売・公売）は非公開リポにだけ存在する。公開ファイルには書かない（7章）

---

## 7. 集計と定義（aggregate.py）

競売のコホート＝「裁判所×開札期日」。公売・国有・公有は「実施機関×開札日（先着順は契約日）」。
統計は開札日の属する月に入れる。

| 指標 | 定義 |
| --- | --- |
| 公告件数 `listed` | そのコホートで最初に見えた物件数 |
| 取下げ件数 `withdrawn` | 開札前に一覧から消え、取下げ・中止と確認できたもの |
| 成立率 `sold_rate` | 売却 ÷ (listed − withdrawn) |
| 取下げ率 `withdrawn_rate` | withdrawn ÷ listed |
| 倍率 `ratio` | sale_price ÷ base_price。中央値と平均、その件数 `n_priced` |
| 特別売却移行率 `special_rate` | 不売 ÷ (listed − withdrawn)（競売だけ） |
| 入札本数 `bidders_median` | 載っている場合だけ |
| 円/㎡ `psm_median` | price_per_sqm の中央値（国有・公有だけ） |

`monthly.json` の1行（`system` ごとに出す）:

```json
{"ym": "2026-09", "system": "keibai", "seller": "神戸地方裁判所尼崎支部", "pref": "兵庫県", "city": "西宮市", "kind": "マンション",
 "listed": 5, "withdrawn": 1, "sold": 4, "unsold": 0, "n_priced": 4,
 "ratio_median": 1.52, "ratio_mean": 1.60, "bidders_median": 6, "psm_median": null}
```

`current.json` は「いま出ている」を 段×売り手×市区町村×種別 の件数と入札期間だけで持つ。競売・公売は住所・事件番号なし。

秘匿ルール（**必ず unittest で固定する**。競売・公売に適用）:

- `n_priced < 3` のとき `ratio_median` / `ratio_mean` / `bidders_median` は `null`
- 件数（`listed` など）は出してよい
- 個別の倍率・価格は公開ファイルに書かない
- 公開ファイルに `case_no`、`key`、市区町村より細かい地名を書かない
- **横棒の幅を「値 ÷ その表の最大値」で出さない。** 最大値は実在の升の値なので、
  幅に掛けるだけで伏せた升が戻る。分母は固定の目盛りにし、伏せた升は棒を出さない
- **階級の下端は3から。** 1〜2件を伏せるなら、境を1と2の間に通さない。
  境が2にあると、画面が「1-2」と書いた真横で色と凡例が1か2かを答える
- 並び順・位置も読者が目で読める数字。件数順に並べ替えない
  （いまの出力は全部キー順に固定してある。崩さない）

詳しくは [docs/kiji-no-tane.md](docs/kiji-no-tane.md) の
「数字を書かなくても、目で読める形は数字を出している」（正本 3.2）。

国有・公有は個票を出す段なので秘匿ルールは適用しないが、`winner_name` の個人名禁止（6章）は公開ファイルにも適用する。
`data/public/kokuyu.json` / `koyu.json` は rows から `docs` のURLと公開してよい項目だけを写したもの。

---

## 8. ページ（build.py）

Phase 3 の公開ページは1枚: `keibai/index.html`。JavaScript なし。グラフは Python でインラインSVGを書く。
見出しは仮に「大阪・兵庫 役所の土地ウォッチ」。専門用語には短い説明を添える（12章）。
「試作版」と明記する（10章の確認が済むまで）。

上から:

1. **但し書き**（3行）: 競売・公売は集計だけで個別の物件は載せない／BITに載らない売却もある（神戸地裁は全件をBITに出していない）／出典と更新日時
2. **棚の全体**: 4段それぞれの「いま出ている件数」と「次の入札日・開札日」
3. **ブロック×5**（各ブロック＝見出し1行＋数字1つ＋グラフ1本＋但し書き1行）

   **見出しは「そのデータが何であるか」を書く。「何に使えるか」ではない**
   （正本 3.3）。同じ数字でも、見出しで性質が変わる。
   2026-09-18 に書き直した。**前の案は5つとも「何に使えるか」だった。**

   | 前の案（使わない） | いまの見出し |
   | --- | --- |
   | 競売にかけられたら、いくらで売られる？ | 売却価額 ÷ 売却基準価額 の中央値（種別別） |
   | **任意売却は間に合う？** | 公告に出た物件のうち、開札まで進んだ物件の割合 |
   | 競売で家は安く買える？ | 倍率の分布と、入札本数の中央値 |
   | 役所の土地は安いのか？ | 4段それぞれの 落札 ÷ 基準価格 |
   | 跡地はいくらで売れた？ | 国有・公有の 円/㎡ の中央値 |

   **「任意売却は間に合う？」がいちばん危なかった。** 一覧に出るのは
   「取下」の2文字だけで、**理由は読めていない**（任意売却がまとまったのか、
   債務者が払ったのか、別の理由か）。読めていない理由を見出しにすると、
   数字が理由の証拠に見える。正本 3.3「評価しない」に触れる。

   「安く買える」「狙い目」は `docs/kiji-no-tane.md` の
   「出さないと決めたもの」に載せてある。**ここと食い違っていた。**
4. **競売**: 市区町村×月の件数表（直近12か月、種別は合計）
5. **公売**: いま出ている件数と入札期日、各サイトへのリンク（住所なし）
6. **国有財産・公有財産**: いま出ている物件の一覧（住所・面積・最低売却価格・入札期間・元ページへのリンク）と、直近の結果一覧
7. **出典**（BIT、国税庁公売情報、近畿財務局、各自治体）、**作法**（robots.txt 遵守・1日1回・3点セットは取らない）、**免責**

Phase 4 で足すもの: 国有・公有の個票ページ（`kokuyu/<key>.html`, `koyu/<key>.html`）、市区町村ページ、裁判所ページ。

---

## 9. 収集の流れ

毎日（Actions、07:30 JST）:

1. robots.txt と応答の確認（recon 相当）
2. `bit-schedule` 7庁 → raw 保存 → parse
3. `bit-list` / `bit-result` / `bit-withdrawn`（自動化できた分）→ raw 保存 → parse → rows 更新
   （key で突合、status 遷移、`last_seen` 更新）
4. `inbox/` に新しいファイルがあれば取り込む。**取り込んでも消さない・移さない。**
   一覧は上書きされるので、その日のスナップショットを残さないとフローが数えられない。
   取り込めたものは `data/inbox-ledger.json` に書いて二度と催促しない
5. 一覧から消えた物件 → 取下げ等の検索で裏取り。自動化できなければ status「消えた」で保留し、レポートに出す
6. 公売 5〜9本 → raw → parse → rows
7. 国有財産 3〜4本、公有財産 6〜10本 → raw → parse → rows（新しい回のページがあればリンクを1段だけ辿る。2MB以下のPDFだけ取る）
8. aggregate → build → publish（差分があるときだけ push）
9. レポートを `GITHUB_STEP_SUMMARY` に出す。取れなかった収集先は太字。PDFを弾いた件数も出す

負荷の目安: 1日50〜60リクエスト、5秒間隔（正本 3.4）、数MB。3点セットを取らないので転送量は小さい。

### inbox（手保存）の決まり

- パス: `inbox/<system>/<id or court_id>/<YYYY-MM-DD>-<kind>.html`（競売の kind = list / result / past / withdrawn）
- 保存のしかた: ブラウザで表示件数を「全て」にしてから「Webページ、HTMLのみ」で保存
- parse は inbox と data/raw を**同じ関数**で読む（出どころが違うだけで形は同じ）
- 競売の過去データは種別ごとに分けて保存: `<日付>-past-<種別>.html`
- 兵庫県の県有地のように数字がPDFの中にしかない場合は、人が inbox に `<日付>-manual.json` を置く（項目は6章と同じ）

### 初回バックフィル（人がやる。1回だけ）

- BIT「過去データの照会」で、裁判所×種別ごとに年指定で3年分を検索 → inbox に保存
- 近畿財務局と大阪府・兵庫県・神戸市の「結果」ページに残っている過去の回も保存（残っている分だけ）
- 取り込み後、rows の件数が各ページに表示される「対象件数」と一致することを確認する

---

## 10. 法務・作法メモ（コードにも README にも反映する）

弁護士に確認する点（ここでは結論を出さない。公開ページには確認が済むまで「試作版」と書く）:

1. BIT利用条件の「本来の趣旨以外の目的」に、一覧の数値を集計して統計にする行為が含まれるか。
   同意ボタンなしの規約の拘束力
2. 広告収益（営利）だと「目的外」と見られやすくなるか
   （**ここが済むまで収入の形は動かさない。** 順番を逆にすると、
   決めたあとで「役所にそう書いてしまっていた」が出る。正本 7節）。
   **屋号「鯨屋」として2通の手紙が出ている**（2026-09-16。大阪府／国立国会図書館）。
   このサイトは1通も出していないが、**同じ相手への説明なので縛られる。**
   国立国会図書館あてには「現時点で、データそのものの販売は予定しておりません」と
   書いてある。`docs/letters/README.md` を先に読むこと
3. 「市区町村＋競売の事実＋価格帯」という粒度の公開でプライバシー・名誉毀損の懸念が消えるか（秘匿ルールの妥当性）
4. 非公開保存でも個人情報取扱事業者の義務（利用目的の特定・法が求める管理・漏えい対応）がどこまで乗るか
5. 公売公告PDFに所有者名が載る場合の扱い
6. 国有・公有の開札結果に載る落札者名の扱い（法人名だけ載せる線でよいか）
7. アクセス頻度の目安と、BITへ事前に統計利用の可否を問い合わせるべきか

規約の確認（人が1回ずつ読んで note に書く）:

- 財務省・近畿財務局のサイト利用規約（政府標準利用規約系の見込み。要確認）
- 大阪府・兵庫県・神戸市・堺市の著作権ページ。大阪市はCC BY 4.0（ページごとに再確認）
- 財務省の売却情報サイト（アットホーム基盤）と KSI は取らない

作法（`shutten/recon.py` から引き継ぐ）:

- robots.txt で拒否 → 取らない。403/404 → 翌日まで待つ。
  海外IPで応答なし（大阪市の前例）→ inbox 運用に切替。参照元ヘッダーの偽装・UA偽装・別経路はしない
- BIT はアクセスログを保守・セキュリティ・統計に使うと明記している。
  UA は `common/site.json` の1か所から配る（正本 3.4）。実物は
  `kujiraya archive bot (keibai-toukei; <連絡フォームURL>)`。**ここに直書きしない**
- `WAIT = 5`（正本 3.4 で5秒以上）、`TIMEOUT = 40`、`socket.setdefaulttimeout` は shutten と同じ

---

## 11. 段階と完了条件

### Phase 0 偵察（1〜2日）

- 非公開リポを作る（人。当時の名前は `keibai-data`）。`sources.json`（5章、4段すべて）と `recon.py`。Actions で1回手動実行
- 完了: `data/recon-report.md` に全収集先の robots／応答／判定（表・PDF・わからない）が出ている。
  3点セットを1本も取っていない。自動化できる kind と inbox に回す kind が決まっている。13章の未確認が埋まっている

### Phase 1 取り込み（2〜3日）

- `parse.py`（bit-past, bit-schedule, bit-result, bit-list, kobai-nta, kokuyu-list, kokuyu-result）。fixtures は inbox に手で置いた実物
- 完了: 競売3年分の rows が入り、件数が「対象件数」と一致。競売・公売の rows に町丁目以下が無いことをテストが保証。
  国有財産の rows に住所・面積・最低売却価格が入る。key の重複なし

### Phase 2 集計（1日）

- `aggregate.py` と秘匿ルール、`winner_name` の個人名禁止。unittest で定義（7章）を固定
- 完了: `data/agg/monthly.json`（社内）と `data/public/index.json`（公開）が出て、競売の n<3 の価格が null

### Phase 3 ページ（1〜2日）

- `build.py`、公開用の workflow。deploy key（公開鍵は収集用の Deploy keys、秘密鍵は公開用の Secret `RAW_DEPLOY_KEY`）は人が入れる
- 完了: 公開用の `index.html` が Pages で見える。JSなし。4段の件数・問いのブロック・出典・但し書き・試作版の明記あり

### Phase 4（後日）

- 公有財産の自治体を府県・政令市から市町へ広げる（5.5の索引から偵察）
- 国有・公有の個票ページ
- bit-list / bit-result の自動化（偵察の結果しだい）、取下げの裏取りの自動化、公売の自治体分
- 市区町村ページ・裁判所ページ、月次レポート記事
- 円/㎡を公示地価・路線価と並べる（データの出どころと規約を別途確認してから）

---

## 12. 用語（ページの脚注に使う）

| 用語 | 短い説明 |
| --- | --- |
| 3点セット | 物件明細書・現況調査報告書・評価書。裁判所が作る競売物件の説明書 |
| 売却基準価額 | 競売で裁判所が決めた目安の値段 |
| 買受可能価額 | 売却基準価額の8割。これ以上なら入札できる |
| 見積価額 | 公売の最低価格 |
| 最低売却価格・予定価格 | 国有財産・公有財産の一般競争入札の最低価格。これ以上で最高額の人が買う |
| 倍率（乖離率） | 落札価額÷基準の値段。何倍で落ちたか。4段で同じ物差し |
| 期間入札 | 決まった期間に札を入れる方式 |
| 特別売却 | 競売で売れ残った物件を先着順で売る方式 |
| 先着順売却 | 国有・公有で入札に出して売れ残った物件を、公表した価格で先着1者に売る方式 |
| 開札期日 | 札を開けて落札者を決める日 |
| 取下げ | 債権者が競売をやめること（任意売却に切り替わった等） |
| 公売 | 税金の滞納で差し押さえた財産を国や自治体が売ること |
| 国有財産の売払い | 国が使わなくなった宿舎跡地・庁舎跡地や、相続税の物納・国庫帰属で国のものになった土地を財務局が売ること |
| 公有財産の売却 | 学校跡地・施設跡地・道路の残地などを自治体の管財課が売ること |
| 物件調書 | 国有・公有の入札で役所が出す物件の説明書 |
| サウンディング | 売る前に事業者の意見を聞く手続き（取らない） |
| コホート | 同じ開札回にまとめた物件の集まり |

---

## 13. 未確認のこと（Phase 0 で埋める。推測で埋めない）

競売:

- BIT の robots.txt の内容
- GitHub Actions（海外IP）から BIT が応答するか
- ~~物件検索・売却結果・過去データ・取下げ等の検索が GET で再現できるか~~
  → **確認ずみ（2026-09-15）。GET では再現できない**（フォーム遷移が要る）ので inbox 運用にした。
  一覧の入口は `app/court/ps004/h04`（競売物件検索・裁判所から探す）で、
  裁判所を選んで検索したあとの画面にしか物件が出ない。1ページの表示上限は既定10件
- 売却結果の列（入札本数が載るか）
- 売却結果が過去データへ移るまでの日数
- 大阪地裁3庁・豊岡支部の court_id
- ~~姫路支部 33332 の確認~~ → **確認ずみ（2026-09-16）。**
  人が保存した競売物件検索の画面に courtId_33332 と「神戸地方裁判所姫路支部」が対で入っていた

公売:

- 国税庁 不動産検索の地域パラメータ（大阪・兵庫）
- 自治体4サイトの利用規約と、公売公告PDFの記載項目（所有者名の有無）
- 神戸市の公売ページURL

国有財産:

- 近畿財務局の先着順（すぐに購入できる物件）ページのURL
- 近畿財務局トップの新URL（旧 `kinki.mof.go.jp` からの移行先）
- 開札結果ページの列（落札価格・落札者名・応札数の有無）と、過去の回がどこまで残っているか
- 財務省・財務局サイトの利用規約

公有財産:

- 大阪市「市有不動産売り払い」と堺市のURL
- 各自治体の「結果」がどのページに出るか（契約課の入札結果など）
- 兵庫県の県有地は数字がPDFの中だけか（HTML一覧の有無）
- 各自治体の著作権ページ

---

## 14. Claude Code への進め方

- まず `shutten/README.md`、`shutten/sources.json`、`shutten/recon.py` を読み、部品と作法を引き継ぐ
- ローカルからは実サイトに取りに行かない。開発は fixtures で。取得は Actions
- 決定事項（1章）に反する近道（3点セットを取る、競売・公売の住所を残す、公開リポに raw を置く、回り込む、落札者の個人名を入れる）はしない。
  必要だと思ったら止めて相談する
- 4段は同じ行データ・同じ parse の枠組みで扱い、段ごとの違いは `system` と公開方針（3.2）で切り替える
- 1 Phase ずつ進め、各 Phase の完了条件を満たしてから次へ
- 分からないこと・未確認のことは推測で埋めず、13章に追記して人に聞く
- README は shutten と同じく、専門用語に短い説明をつけ、「なぜそうしたか」を残す
