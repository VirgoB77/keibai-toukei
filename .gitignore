__pycache__/
*.pyc
.DS_Store

# ブラウザが「完全」で保存したときに一緒にできる画像やCSSの入れ物。
# 読むのはHTMLだけなので、置いてあっても無視する
inbox/**/*_files/

# 予約台帳（VirgoB77/kujiraya-aite-yoyaku・private）を Actions が隣に出す場所。
# 手で git add -A したときに、予約台帳の中身がこちらに入るのを防ぐ
_yoyaku/
